// reaction will not be a model but used as the reaction field in subdocument schema in the thought model
const User = require('./user');
const Thought = require('./thought');

module.exports = { User, Thought };